import { ANSWER_QUESTION } from '../action/answers'
import { UPDATE_AUTHOR_QUESTION } from '..action/question'

export default function answers(state = {}, action) {
    switch(action.type) {
        default:
            return state
    }
}

